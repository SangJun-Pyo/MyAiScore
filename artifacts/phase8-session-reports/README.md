# Phase 8 verification artifacts

All data here is synthetic. No personal Claude Code transcript was read or copied.

- synthetic-example.json: generated by the real CLI with --example --out, Curious explorer / 65. Can be opened with the new browser report input; cannot be saved as personal history.
- report-desktop.png / report-mobile.png: browser import regression with invented local-origin counts (Hands-on builder / 45), not the user's session.
- home-desktop.png / new-session-mobile.png: primary product routes from the successful production browser suite.

Reproduce the CLI example from an installed checkout with npm run session:report -- --example. To write JSON, choose a new output path: --out refuses to overwrite an existing file.
