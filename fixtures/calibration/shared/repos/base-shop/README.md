# base-shop (synthetic fixture)

A tiny synthetic Next.js/TypeScript project used by several MyAiScore
calibration fixtures. It is not a real product. It exists only so multiple
calibration cases can point at the *same* underlying code while attaching
different AI-collaboration narratives to it.
